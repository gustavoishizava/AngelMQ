trigger:
  branches:
    exclude:
      - '*'
  tags:
    include:
      - '*'

pr: none

pool:
  vmImage: 'ubuntu-24.04'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'
  dotnetSdkVersion: 8.0.x
  packageProject: '**/AngelMQ.csproj'
  unitTestPathPattern: '**/*UnitTests/*.csproj'
  previewCounter: $[counter(variables['Build.SourceBranchName'], 1)]
  packageVersion: '$(Build.SourceBranchName)-preview$(previewCounter)'

stages:
  - stage: Build_and_Test
    displayName: 'Build and Test'
    jobs:
      - job: Build
        displayName: 'Build and test'
        pool:
          vmImage: 'ubuntu-24.04'
        steps:
          - task: UseDotNet@2
            displayName: 'Install .NET Core SDK'
            inputs:
              version: '$(dotnetSdkVersion)'
              performMultiLevelLookup: true
          - task: DotNetCoreCLI@2
            displayName: 'Dotnet Restore'
            inputs:
              command: 'restore'
              projects: '$(solution)'
              feedsToUse: 'select'
          - task: DotNetCoreCLI@2
            displayName: 'Dotnet build $(buildConfiguration)'
            inputs:
              command: 'build'
              projects: '**/*.csproj'
              arguments: '--configuration $(buildConfiguration)'
          - task: DotNetCoreCLI@2
            displayName: 'Run unit tests'
            inputs:
              command: test
              projects: '$(unitTestPathPattern)'
              arguments: '--configuration $(buildConfiguration) /p:CollectCoverage=true /p:CoverletOutputFormat=opencover --collect:"XPlat Code Coverage"'
          - task: PublishCodeCoverageResults@1
            displayName: 'Publish code coverage report'
            inputs:
              codeCoverageTool: 'Cobertura'
              summaryFileLocation: '$(Agent.TempDirectory)/**/coverage.cobertura.xml'

  - stage: NuGet_Pack_And_Push_Preview
    displayName: 'NuGet Pack and Push Preview'
    dependsOn: Build_and_Test
    condition: succeeded()
    jobs:
      - deployment: Deploy
        displayName: 'Pack and Push to NuGet'
        pool:
          vmImage: 'ubuntu-24.04'
        environment: 'NuGetPreview'
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self
                - task: UseDotNet@2
                  displayName: 'Install .NET Core SDK'
                  inputs:
                    version: '$(dotnetSdkVersion)'
                    performMultiLevelLookup: true
                - task: NuGetAuthenticate@1
                  displayName: 'Authenticate to NuGet.org'
                  inputs:
                    nuGetServiceConnections: 'Nuget Org'
                - task: DotNetCoreCLI@2
                  displayName: 'Pack NuGet Package'
                  inputs:
                    command: 'pack'
                    packagesToPack: '$(packageProject)'
                    versioningScheme: 'byEnvVar'
                    versionEnvVar: 'PACKAGE_VERSION'
                  env:
                    PACKAGE_VERSION: '$(packageVersion)'
                - script: |
                    dotnet nuget push $(Build.ArtifactStagingDirectory)/*.nupkg --source "Nuget Org" --api-key az --skip-duplicate
                  displayName: 'Push to NuGet.org'
          